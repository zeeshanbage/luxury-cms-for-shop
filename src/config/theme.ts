export const themeConfig = {
  // Theme branding colors (Hex values injected as custom properties at runtime)
  colors: {
    primary: "#C5A880",       // Champagne Gold
    primaryHover: "#D4AF37",  // Bright Metallic Gold
    primaryMuted: "#5c4f3c",  // Muted Bronze Gold
    background: "#030303",     // Pitch Black / Obsidian
    card: "#09090b",           // Deep Slate Black Card
    border: "#1a1a1e",         // Subtle Border Gray
    glowColor: "rgba(197, 168, 128, 0.15)", // Glow Shadow color mapping
  }
};
